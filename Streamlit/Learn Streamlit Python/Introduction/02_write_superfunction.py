import streamlit as st
import numpy as np

# write can do many things
st.write("This is text")
st.write("# This is markdown text")
st.write(1+2+10)

st.write(dir(st))

# help for methods and functions
st.help(range)
st.help(np.arange)
